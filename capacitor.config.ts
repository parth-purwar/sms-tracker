import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.smsspendtracker',
  appName: 'smsspend-tracker',
  webDir: 'dist'
};

export default config;
